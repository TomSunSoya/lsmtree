# AGENTS.md

项目主干已经完成，目前处于收尾、复盘和维护阶段。

- 开始工作前先读 `CLAUDE.md`，不要从头重做设计。
- 可以直接完成用户明确要求的 bug 修复、回归测试、可读性重构和文档整理。
- 除非用户明确要求，不新增并发、事务、压缩、SQL、网络服务等功能，也不改变公开 API 或持久化格式。
- 修改存储路径时，保持 WAL、SSTable、Manifest 的写入、同步、发布和清理顺序；同一格式尺寸只能有一个真相源。
- 保留工作树中与当前任务无关的改动，不擅自清理、回退或提交。
- 简单任务由主 agent 完成，不使用 subagent。
- 提交前必须运行完整测试；涉及内存生命周期、二进制解析或持久化时，再运行 ASan/UBSan。
